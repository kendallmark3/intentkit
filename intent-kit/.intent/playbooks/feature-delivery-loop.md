# Feature Delivery Loop

Intent -> Refine -> Context -> Plan -> Tasks -> Implement -> Verify -> Evidence -> Impact -> Learn.

Every phase produces an artifact. Every artifact should be readable by a human and useful to an agent.
