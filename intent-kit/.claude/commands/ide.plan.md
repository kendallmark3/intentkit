# ide.plan

Create the implementation plan.

Instructions:
1. Read `intent.md`, `repo-context.md`, and `.intent/memory/*`.
2. Update `architecture.md` with impact map and key decisions.
3. Update `plan.md` with phases, files expected to change, dependencies, rollback plan, and human review points.
4. Do not code.
5. End with recommended next command: `ide.tasks`.
